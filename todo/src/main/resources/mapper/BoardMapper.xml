<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.domain.todo.board.mapper.BoardMapper">

    <select id="getTodoList" parameterType="map" resultType="map">
        SELECT
            TODO_ID,
            CONTENT,
            DATE_FORMAT(REGIST_DATE, '%Y-%m-%d') AS registDate,
            CASE
                WHEN STATUS_YN = 'Y' THEN 1
                WHEN STATUS_YN = 'N' THEN 0
                ELSE 0
            END AS STATUS
        FROM BOARD
        <where>
            DELETE_FLAG_YN = 'N'
            <if test="search != null and search != ''">
                AND CONTENT LIKE CONCAT('%', #{search}, '%')
            </if>
            <if test="status != null">
                AND STATUS_YN = CASE WHEN #{status} = 1 THEN 'Y' ELSE 'N' END
            </if>
        </where>
        ORDER BY
            CASE WHEN STATUS_YN = 'N' THEN 0 ELSE 1 END,
            REGIST_DATE DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="getTodoListCnt" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM BOARD
        <where>
            DELETE_FLAG_YN = 'N'
            <if test="search != null and search != ''">
                AND CONTENT LIKE CONCAT('%', #{search}, '%')
            </if>
            <if test="status != null">
                AND STATUS_YN = CASE WHEN #{status} = 1 THEN 'Y' ELSE 'N' END
            </if>
        </where>
    </select>

    <select id="getTodoDetail" parameterType="int" resultType="map">
        SELECT
            TODO_ID,
            CONTENT,
            DATE_FORMAT(REGIST_DATE, '%Y-%m-%d') AS registDate,
            CASE
                WHEN STATUS_YN = 'Y' THEN 1
                WHEN STATUS_YN = 'N' THEN 0
                ELSE 0
            END AS STATUS
        FROM BOARD
        WHERE TODO_ID = #{todoId}
            AND DELETE_FLAG_YN = 'N'
    </select>

    <insert id="addTodo" parameterType="map" useGeneratedKeys="true" keyProperty="todoId">
        INSERT INTO BOARD (
            CONTENT,
            STATUS_YN
        )
        VALUES (
            #{content},
            CASE
                WHEN #{status} = 1 THEN 'Y'
                WHEN #{status} = 0 THEN 'N'
                ELSE 'N'
            END
        )
    </insert>

    <update id="updateTodo" parameterType="map">
        UPDATE BOARD
        SET
            CONTENT = #{content},
            STATUS_YN = CASE
                WHEN #{status} = 1 THEN 'Y'
                ELSE 'N'
            END
        WHERE TODO_ID = #{todoId}
    </update>

    <update id="updateTodoStatus" parameterType="map">
        UPDATE BOARD
        SET
            STATUS_YN = CASE
                WHEN #{status} = 1 THEN 'Y'
                ELSE 'N'
            END
        WHERE TODO_ID = #{todoId}
    </update>

    <update id="deleteTodo" parameterType="int">
        UPDATE BOARD
        SET
            DELETE_FLAG_YN = 'Y'
        WHERE TODO_ID = #{todoId}
            AND DELETE_FLAG_YN = 'N'
    </update>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.domain.todo.auth.mapper.AuthMapper">

    <select id="findByProviderAndId" resultType="map">
        SELECT
            user_id,
            display_name,
            email,
            provider,
            provider_id
        FROM user
        WHERE provider = #{provider}
        AND provider_id = #{providerId}
    </select>

    <insert id="insertUser" parameterType="map" useGeneratedKeys="false">
        INSERT INTO user(
            user_id,
            display_name,
            email,
            provider,
            provider_id,
            created_by
        ) VALUES(
                 #{user_id},
                 #{display_name},
                 #{email},
                 #{provider},
                 #{provider_id},
                 #{created_by}
        )
    </insert>

</mapper>